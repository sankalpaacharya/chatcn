import CodePreview from "@/components/code-preview";
import DocHeader from "@/components/doc-header";
import CodeBlock from "@/registry/new-york/codeblock/codeblock";
import { calendarCode } from "@/lib/utils";
import CommandBlock from "@/registry/new-york/command-tabs/command-tabs";

<DocHeader
  title="Code Block"
  description="Display syntax-highlighted code snippets using Shiki."
  links={[{ label: "Shiki Reference", href: "https://shiki.style/packages/next" }]}
/>

<div className="space-y-10">

---
{/*  i made too many commits for random shit just squash them later please */}
## 📦 Installation

Install this component using the CLI:
<div>
<CommandBlock
  title="Install Component"
  defaultValue="pnpm"
  commands={[
    {
      label: "npm",
      command:
        "npx shadcn@latest add https://shadcn-collections.vercel.app/r/codeblock.json",
    },
    {
      label: "yarn",
      command:
        "yarn dlx shadcn@latest add https://shadcn-collections.vercel.app/r/codeblock.json",
    },
    {
      label: "pnpm",
      command:
        "pnpm dlx shadcn@latest add https://shadcn-collections.vercel.app/r/codeblock.json",
    },
    {
      label: "bun",
      command:
        "bunx --bun shadcn@latest add https://shadcn-collections.vercel.app/r/codeblock.json",
    },
  ]}
/>
</div>
---

## 🔍 Preview

<CodeBlock lang="tsx">{`
import React, { useState } from "react";

function Greeting() {
  const [show, setShow] = useState(true);

  return (
    <div>
      {show && <h1>Hello, Sanku! 👋</h1>}
      <button onClick={() => setShow((prev) => !prev)}>
        {show ? "Hide" : "Show"} Greeting
      </button>
    </div>
  );
}

export default Greeting;
`}</CodeBlock>

---

## 🛠️ Usage

Here’s how you use the `CodeBlock` component:

<CodeBlock lang="tsx">{`
import CodeBlock from "@/components/codeblock";

export default function Page() {
  return (
    <CodeBlock lang="tsx">{\`
function Greeting() {
  const [show, setShow] = useState(true);

  return (
    <div>
      {show && <h1>Hello, Sanku! 👋</h1>}
      <button onClick={() => setShow((prev) => !prev)}>
        {show ? "Hide" : "Show"} Greeting
      </button>
    </div>
  );
}

export default Greeting;
\`}</CodeBlock>
  );
}
`}</CodeBlock>

</div>
