# Tool Call

Display tool call states and results.

<ComponentPreview component="tool"/>

<br/>

### Different States

Tool states during execution: `Processing`, `Completed`, and `Error`.

<ComponentPreview component="tool-states"/>

<br/>

## Installation

<Installation component={"tool"}/>

<br/>

## API

### Tool

<p className="text-muted-foreground text-sm mb-6">Tool call display with state management.</p>

<div className="not-prose">
  <dl className="divide-y divide-border">
    <div className="py-4 flex flex-col sm:flex-row sm:gap-8">
      <dt className="font-mono text-sm font-medium min-w-[160px] mb-1 sm:mb-0">name</dt>
      <dd className="text-sm text-muted-foreground">Tool name to display <span className="text-foreground/60">· string</span></dd>
    </div>
    <div className="py-4 flex flex-col sm:flex-row sm:gap-8">
      <dt className="font-mono text-sm font-medium min-w-[160px] mb-1 sm:mb-0">state</dt>
      <dd className="text-sm text-muted-foreground">Current state of tool call <span className="text-foreground/60">· "LOADING" | "COMPLETED" | "ERROR"</span></dd>
    </div>
    <div className="py-4 flex flex-col sm:flex-row sm:gap-8">
      <dt className="font-mono text-sm font-medium min-w-[160px] mb-1 sm:mb-0">input</dt>
      <dd className="text-sm text-muted-foreground">Input parameters <span className="text-foreground/60">· Record&lt;string, unknown&gt;</span></dd>
    </div>
    <div className="py-4 flex flex-col sm:flex-row sm:gap-8">
      <dt className="font-mono text-sm font-medium min-w-[160px] mb-1 sm:mb-0">output</dt>
      <dd className="text-sm text-muted-foreground">Output from execution <span className="text-foreground/60">· Record&lt;string, unknown&gt;</span></dd>
    </div>
    <div className="py-4 flex flex-col sm:flex-row sm:gap-8">
      <dt className="font-mono text-sm font-medium min-w-[160px] mb-1 sm:mb-0">errorText</dt>
      <dd className="text-sm text-muted-foreground">Error message when state is ERROR <span className="text-foreground/60">· string</span></dd>
    </div>
    <div className="py-4 flex flex-col sm:flex-row sm:gap-8">
      <dt className="font-mono text-sm font-medium min-w-[160px] mb-1 sm:mb-0">children</dt>
      <dd className="text-sm text-muted-foreground">Additional trigger content <span className="text-foreground/60">· ReactNode</span></dd>
    </div>
  </dl>
</div>

<br/>

### ToolStateBadge

<p className="text-muted-foreground text-sm mb-6">State indicator badge. Inherits state from parent Tool context.</p>
